# AI Agent 调试协作指南

核心思想：优先用 MCP 在浏览器里自动化复现与验收，不让用户提供技术细节。

- 先运行自动化验收/调试：优先使用 Chrome DevTools MCP；没有的话使用 Playwright MCP。
- 如果当前环境没有合适的 MCP：直接提示用户安装并完成配置后再继续自动化验收。
- 自动化过程中：导航到验收 URL，读取控制台错误/警告，必要时截图/快照，复现交互后修复代码。
- 修复后：再次跑自动化验收，直到状态恢复正常（一次只修一个问题）。
